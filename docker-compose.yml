# ontoMeta 一键部署：PostgreSQL + API + Frontend
# 用法（仓库根目录）：
#   docker compose up --build -d
# 访问：
#   前端 http://localhost:5180
#   API  http://localhost:8000  （/docs、/health）
# 可选 Redis：取消下方 redis 服务注释（当前草稿队列为进程内，不依赖 Redis）

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ontometa
      POSTGRES_PASSWORD: ontometa
      POSTGRES_DB: ontometa
    volumes:
      - ontometa_pg:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ontometa -d ontometa"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: ./backend
    environment:
      DATABASE_URL: postgresql+psycopg://ontometa:ontometa@postgres:5432/ontometa
      ONTOMETA_ADMIN_TOKEN: ${ONTOMETA_ADMIN_TOKEN:-dev-admin-token-change-me}
      DEBUG: ${DEBUG:-false}
      DATAHUB_GMS_URL: ${DATAHUB_GMS_URL:-http://host.docker.internal:8080}
      DATAHUB_FRONTEND_URL: ${DATAHUB_FRONTEND_URL:-http://localhost:9002}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      API_KEY_HASH_PEPPER: ${API_KEY_HASH_PEPPER:-}
      MAX_CONCURRENT_DRAFT_GENERATIONS: ${MAX_CONCURRENT_DRAFT_GENERATIONS:-2}
      EXTERNAL_API_RATE_LIMIT_PER_MINUTE: ${EXTERNAL_API_RATE_LIMIT_PER_MINUTE:-60}
      CUBE_API_URL: ${CUBE_API_URL:-http://cube:4000}
      CUBE_API_SECRET: ${CUBE_API_SECRET:-}
      CUBE_PREAGG_REFRESH: ${CUBE_PREAGG_REFRESH:-1 hour}
      CUBE_TENANT_DIMENSION: ${CUBE_TENANT_DIMENSION:-}
      CORS_ORIGINS: '["http://localhost:5180","http://127.0.0.1:5180"]'
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
    ports:
      - "5180:80"
    depends_on:
      - api

  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"

  # 可选：Cube 语义层（外挂执行/缓存/预聚合）。启用后填写 CUBE_API_SECRET。
  # ontoMeta 通过 GET /api/ontologies/{id}/cube-model 生成 model，落到 ./cube/model 后由 Cube 加载。
  # cube:
  #   image: cubejs/cube:latest
  #   ports:
  #     - "4000:4000"
  #   environment:
  #     CUBEJS_DEV_MODE: "true"
  #     CUBEJS_API_SECRET: ${CUBE_API_SECRET:-change-me}
  #     CUBEJS_DB_TYPE: postgres
  #     CUBEJS_DB_HOST: postgres
  #     CUBEJS_DB_NAME: ontometa
  #     CUBEJS_DB_USER: ontometa
  #     CUBEJS_DB_PASS: ontometa
  #   volumes:
  #     - ./cube:/cube/conf
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  # cube_refresh_worker:
  #   image: cubejs/cube:latest
  #   environment:
  #     CUBEJS_API_SECRET: ${CUBE_API_SECRET:-change-me}
  #     CUBEJS_REFRESH_WORKER: "true"   # 预聚合定时刷新（替代进程内 cron）
  #     CUBEJS_DB_TYPE: postgres
  #     CUBEJS_DB_HOST: postgres
  #     CUBEJS_DB_NAME: ontometa
  #     CUBEJS_DB_USER: ontometa
  #     CUBEJS_DB_PASS: ontometa
  #   volumes:
  #     - ./cube:/cube/conf
  #   depends_on:
  #     - cube

volumes:
  ontometa_pg:
