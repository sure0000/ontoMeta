fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36
alembic>=1.14.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
httpx>=0.28.0
python-dotenv>=1.0.1
openai>=1.57.0
sqlparse>=0.5.0
sqlglot>=25.0.0
Jinja2>=3.1.0
paramiko>=3.4.0
python-multipart>=0.0.9   # 血缘补录的代码包上传（multipart，避免 base64 撑爆 nginx 体积限制）
PyYAML>=6.0
pytest>=8.0.0

# --- MCP Server ---
# 2.x 起低层 Server 收 on_list_tools / on_call_tool 回调（1.x 是 @server.list_tools()
# 装饰器，且 FastMCP 已更名 MCPServer）——app/mcp/server.py 按 2.x 写，别放宽下界。
mcp>=2.1.0              # Model Context Protocol SDK

# --- 数仓引擎驱动 ---
# Data App / Chat BI 直连数仓、数据源连接测试时需要。已在本环境装齐。
# Hive / Kyuubi：别用 pyhive[hive] extra——它带的 sasl 是 C 扩展，Python≥3.12
#   编译不过（longintrepr.h 已移除）；pure-sasl 是纯 Python 实现，thrift-sasl
#   会自动改用它。
pymysql>=1.1.0          # MySQL / Doris / StarRocks（走 MySQL 线协议）
psycopg2-binary>=2.9.0  # PostgreSQL（裸 postgresql:// DSN 默认走 psycopg2）
psycopg[binary]>=3.2.0  # PostgreSQL v3 驱动（postgresql+psycopg:// DSN 用）
pyhive>=0.7.0
thrift>=0.20.0
thrift-sasl>=0.4.3
pure-sasl>=0.6.0
clickhouse-sqlalchemy>=0.2.0
duckdb-engine>=0.15.0
