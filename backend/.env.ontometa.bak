DATABASE_URL=sqlite:///./ontometa.db
DATAHUB_GMS_URL=http://localhost:8080

ONTOMETA_ADMIN_TOKEN=dev-admin-token-change-me

# V5 P1：Data Agent 运行轨迹（JSONL）——观测用，非常驻。P1 实测已完成，已关回。
# 需再采时改回 true，重启后生效（轨迹写 backend/.logs/agent_traces/）。
AGENT_TRACE_ENABLED=false
AGENT_TRACE_DIR=.logs/agent_traces
